# Core
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
python-multipart>=0.0.9
aiofiles>=23.0.0

# TTS - CosyVoice3 dependencies
# CosyVoice3 本體由 start.bat clone 到 python/CosyVoice/，這裡只列 pip 依賴。
# 版本號對齊 CosyVoice/requirements.txt + third_party/Matcha-TTS/requirements.txt。
# 排除項目：gradio / grpcio / deepspeed / tensorboard / tensorrt（不需要 Web UI / gRPC / 訓練 / Linux-only）
# ── CosyVoice/requirements.txt ──
conformer==0.3.2
diffusers==0.29.0
gdown>=5.1.0
hydra-core>=1.3.2
HyperPyYAML>=1.2.3
inflect>=7.3.1
librosa>=0.10.2
lightning>=2.2.4
matplotlib>=3.7.5
modelscope>=1.20.0
networkx>=3.1
numpy>=1.26.4
omegaconf>=2.3.0
onnx>=1.16.0
onnxruntime>=1.18.0
protobuf>=4.25
pyarrow>=18.1.0
pyworld>=0.3.4
rich>=13.7.1
soundfile>=0.12.1
transformers>=4.40.0
wetext>=0.0.4
wget>=3.2
x-transformers>=2.11.24
# ── Matcha-TTS/requirements.txt (inference 需要) ──
einops
pandas
scipy
Unidecode

# PyTorch / torchaudio 不放在 requirements.txt
# 由 start.bat 依 Windows + CUDA 環境強制安裝相容版本，避免被 pip 自動解析成 CPU 版。

# STT - faster-whisper (CUDA)
faster-whisper>=1.0.0
openai-whisper>=20250625
